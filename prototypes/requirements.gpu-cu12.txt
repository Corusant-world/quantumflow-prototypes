# Prototypes — NVIDIA GPU (CUDA 12) environment
numpy>=1.24.0
torch>=2.0.0
pytest>=7.0.0

# NVML metrics (benchmarks + ecosystem_smoke)
nvidia-ml-py>=12.0.0

# Used by Team3 cuQuantum path and optional GPU probes
cupy-cuda12x>=12.0.0




# Prototypes — CPU-only environment (no CUDA required)
numpy>=1.24.0
torch>=2.0.0
pytest>=7.0.0

# Needed for benchmarks that record NVML metrics (on GPU it uses NVML; on CPU it's harmless)
nvidia-ml-py>=12.0.0



